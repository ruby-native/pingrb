<p class="text-neutral-700">Fire a test event with the <%= link_to "Stripe CLI", "https://docs.stripe.com/stripe-cli", target: "_blank", rel: "noopener", class: "text-emerald-700 hover:underline" %>. Once it's installed and logged in:</p>

<pre class="mt-3 overflow-x-auto rounded-sm bg-neutral-200 px-3 py-2 text-xs text-neutral-900">stripe trigger payment_intent.succeeded</pre>

<p class="mt-3 text-xs text-neutral-500">The notification should land below within a second.</p>
